[project]
name = "spec-flux"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pcarlip", email = "carlipp@oregonstate.edu" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
spec-flux = "spec_flux:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
